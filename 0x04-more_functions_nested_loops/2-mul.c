#include "main.h"

/**
 * mul - function to multiply two numbers
 * @a: the argument of the mul function.
 * @b: the argument of the mul function.
 *
 * Return: Always 0.
*/

int mul(int a, int b)
{
	return (a * b);
}
