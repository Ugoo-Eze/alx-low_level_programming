#include "main.h"

/**
 * _islower - function to check lowercase character.
 * @c: the argument of the _islower function.
 *
 * Return: Always 0.
*/

int _isdigit(int c)
{
	if (c >= 0 && c <= 9)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
