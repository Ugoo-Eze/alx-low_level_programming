#include "main.h"

/**
 * swap_int - function to swap two values
 *@a: argument value 1
 *@b: argument value 2
*/

int _strlen(char *s)
{
	int n = 0;

	while (s[n] != '\0')
	{
		n++;
	}

	return (n);
}
