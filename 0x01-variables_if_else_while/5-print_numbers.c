#include <stdio.h>

/**
 * main - program starting point
 *
 * Return: end program
 *
*/

int main(void)
{
	int base;

	for (base = 0; base < 10; base++)
	{
		printf("%d", base);
	}

	printf("\n");
	return (0);
}
